#include <iostream>

using namespace std;

int main() {
    long long int A, B, C, D;
    cin >> A;
    cin.ignore();
    cin >> B;
    cin.ignore();
    cin >> C;
    cin.ignore();
    cin >> D;    
    

    cout << "DIFERENCA = " << ((A*B) - (C*D)) << endl;
    return 0;
}